import { ClassPath } from '../../src/core';

describe('Classpath', () => {

    let classpath: ClassPath;

    beforeEach(() => {
        classpath = new ClassPath();
    });

    /*it('Instanciation du classpath', () => {
        classpath.className = 'className';
        classpath.path = 'path';
        expect(classpath).toBeDefined();
        expect(classpath.className).toEqual('className');
        expect(classpath.path).toEqual('path');
    });*/

});
